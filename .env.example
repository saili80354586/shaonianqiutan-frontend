# API Base URL. Local: http://localhost:8080/api. Production can use /api behind reverse proxy.
VITE_API_URL=http://localhost:8080/api

# WebSocket host. Leave empty in production when API and frontend share a host.
# Example: localhost:8080
VITE_WS_URL=
